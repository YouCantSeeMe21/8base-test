"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
class ChainHandler {
    constructor(handlerFunction) {
        this.handlerFunction = handlerFunction;
    }
    static fromArray(handlerFunctions = []) {
        if (handlerFunctions.length === 0) {
            return new ChainHandler((next, data) => next(data));
        }
        const handlers = handlerFunctions.map(handlerFunction => new ChainHandler(handlerFunction));
        return handlers.reduceRight((nextHandler, handler) => {
            handler.setNext(nextHandler);
            return handler;
        });
    }
    handle(data) {
        return __awaiter(this, void 0, void 0, function* () {
            return this.handlerFunction(data => {
                if (this.next) {
                    return this.next.handle(data);
                }
                return Promise.resolve(data);
            }, data);
        });
    }
    setNext(handler) {
        if (typeof handler === 'function') {
            handler = new ChainHandler(handler);
        }
        this.next = handler;
    }
}
exports.ChainHandler = ChainHandler;
//# sourceMappingURL=ChainHandler.js.map