import { ApiGraphQLError } from './errors/ApiGraphQLError';
import { IErrorCallback, IErrorMap, IRerunFunction } from './types';
import { ExecutionResult } from 'graphql';
export declare class ErrorHandler {
    private readonly errorCallback?;
    private readonly errorMap?;
    constructor(callback?: IErrorCallback | IErrorMap);
    handle(error: ApiGraphQLError, rerun: IRerunFunction): void | Promise<ExecutionResult>;
    private handleWithCallback;
    private handleWithErrorMap;
}
//# sourceMappingURL=ErrorHandler.d.ts.map