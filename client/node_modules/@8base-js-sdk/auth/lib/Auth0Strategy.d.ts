import { Auth0DecodedHash, AuthorizeOptions, LogoutOptions } from 'auth0-js';
import { IAuth0AuthSettings, IAuthStorageState } from './types';
export declare class Auth0Strategy {
    private readonly logoutRedirectUri;
    private readonly auth0Client;
    constructor(settings: IAuth0AuthSettings);
    authorize(provider?: string, options?: AuthorizeOptions): void;
    getAuthorizedData(): Promise<Auth0DecodedHash | null>;
    refresh(): Promise<Auth0DecodedHash | null>;
    signOut(options?: LogoutOptions): void;
    forgotPassword(email: string): Promise<string>;
    convertAuthDataToState(auth0Data: Auth0DecodedHash): IAuthStorageState;
}
//# sourceMappingURL=Auth0Strategy.d.ts.map